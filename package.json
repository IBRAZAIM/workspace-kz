{
  "name": "workspace-kz",
  "version": "1.0.0",
  "description": "Платформа для почасовой аренды кабинетов WorkSpace.kz",
  "main": "backend/server.js",
  "scripts": {
    "setup": "npm install && cd backend && npm install",
    "server": "node backend/server.js",
    "client": "npx serve ./ -l 5500",
    "start": "npx concurrently --kill-others \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "serve": "^14.2.1"
  }
}
